<link rel="stylesheet" type="text/css" href="/assets/jquery-datetimepicker/jquery.datetimepicker.css"/ >
<%= form_with model: @interview, local: true do |form| %>
    
    <% if @interview.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@interview.errors.count, "error") %> prohibited
        this interview from being scheduled:
      </h2>
      <ul>
        <% @interview.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

    <p>
      <%= form.label :candidate %><br>
      <%= form.collection_select :candidate_id, Candidate.all, :id, :first_name %>
    </p>
    <p>
      <%= form.label :manager %><br>
      <%= form.collection_check_boxes :manager_ids, Manager.all, :id, :first_name %>
    </p>
    <p>
      <%= form.label :location %>
      <%= form.text_field :location %>
    </p>
    <p>
      <%= form.label :start_datetime %>
      <%= form.text_field :start_datetime %>
      <script>
      jQuery('#interview_start_datetime').datetimepicker();
      </script>
    </p>
    <p>
      <%= form.label :end_datetime %>
      <%= form.text_field :end_datetime %>
      <script>
      jQuery('#interview_end_datetime').datetimepicker();
      </script>
    </p>
    <p>
      <%= form.submit %>
    </p>

<% end %>